# fragman.yml'yi GÜNCELLE:
name: Fragman Pipeline

on:
  repository_dispatch:
    types: [fragman_request]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Repo Checkout
        uses: actions/checkout@v4

      - name: 🧱 FFmpeg ve YouTube-DL Kur
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg python3 python3-pip
          
          # yt-dlp'yi BOTH şekilde kur
          pip3 install --upgrade yt-dlp
          
          # Ayrıca binary olarak da kur
          sudo wget https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -O /usr/local/bin/yt-dlp
          sudo chmod a+rx /usr/local/bin/yt-dlp
          
          # yt-dlp kurulumunu test et
          yt-dlp --version
          python3 -c "import yt_dlp; print(f'Python yt-dlp versiyon: {yt_dlp.version.__version__}')"

      - name: 🎬 Fragman Üret
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
        run: |
          echo "Başlangıç: $(date)"
          python3 fragman.py
          echo "Bitiş: $(date)"
