name: Fragman Pipeline

on:
  repository_dispatch:
    types: [fragman_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Repo Checkout
        uses: actions/checkout@v4

      - name: 🧱 FFmpeg ve Gerekli Paketler Kur
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg python3-pip

      - name: 🐍 Python Kurulumu
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Python Paketleri Kur (pytube)
        run: |
          pip install pytube requests
          # pytube güncel değilse, şu repo'dan yükle:
          pip install git+https://github.com/pytube/pytube.git

      - name: 🎬 Fragman Üret
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
        run: python fragman.py
